# 本機開發用的後端 API 位址。
# 對應 MyErp.Api/Properties/launchSettings.json 的 https profile（applicationUrl）。
# 如果你的 dotnet run 印出的網址不是這個 port，改這裡就好，不用改程式碼。
#
# 注意：第一次用 https://localhost:7073 連線，瀏覽器可能會擋掉自我簽章憑證，
# 需要先在後端專案跑過一次「dotnet dev-certs https --trust」信任 ASP.NET Core 的開發憑證，
# 或是改用下面註解掉的 http 版本（沒有憑證問題，但要注意 http 版本沒有走 UseHttpsRedirection）。
VITE_API_BASE_URL=https://localhost:7073/api
# VITE_API_BASE_URL=http://localhost:5076/api
